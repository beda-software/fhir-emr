import styled from 'styled-components';

import { Button } from 'antd';

export const S = {
    Actions: styled.div`
        display: flex;
        gap: 8px 16px;
    `,
    LinkButton: styled(Button)`
        padding: 0;
    `,
};
