import styled from 'styled-components';

export const S = {
    Title: styled.div`
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
        gap: 8px;
    `,
};
