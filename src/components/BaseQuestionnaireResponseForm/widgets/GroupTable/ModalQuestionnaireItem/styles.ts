import styled from 'styled-components';

export const S = {
    GroupContent: styled.div`
        display: flex;
        flex-direction: column;
        padding: 24px 0;
        gap: 16px;
        width: 100%;
    `,
};
